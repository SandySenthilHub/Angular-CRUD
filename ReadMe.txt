Steps to Run a Employee CRUD:

Step1: In command Prompt enter <npm install> to download the npm packages used in the application.
Step2: Enter <ng server> inside the crud-app folder to start the Angular.
Step3: Enter <npx json-server --watch db.json> to get the json database employee port. 